# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/*
!.vscode/extensions.json
.idea/

# Backend and frontend have their own .gitignore for vendor/, node_modules/,
# .env, build output, etc. — this file only covers repo-wide clutter, plus the
# root's own node_modules (concurrently, used by `npm run dev`).
/node_modules

